const Navbar = () => {
  return (
    <div className={`p-4 border-b-2`}>
      <div className={`container mx-auto`}>
        <h2 className={`m-0 text-2xl font-semibold`}>Dev Digest</h2>
      </div>
    </div>
  );
};

export default Navbar;
